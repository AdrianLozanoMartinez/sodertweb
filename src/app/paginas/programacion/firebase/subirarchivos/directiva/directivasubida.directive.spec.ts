import { DirectivasubidaDirective } from './directivasubida.directive';

describe('DirectivasubidaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivasubidaDirective();
    expect(directive).toBeTruthy();
  });
});
