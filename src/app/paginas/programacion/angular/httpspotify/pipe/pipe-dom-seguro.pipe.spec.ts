import { PipeDomSeguroPipe } from './pipe-dom-seguro.pipe';

describe('PipeDomSeguroPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeDomSeguroPipe();
    expect(pipe).toBeTruthy();
  });
});
