import { PipespotifyPipe } from './pipespotify.pipe';

describe('PipespotifyPipe', () => {
  it('create an instance', () => {
    const pipe = new PipespotifyPipe();
    expect(pipe).toBeTruthy();
  });
});
