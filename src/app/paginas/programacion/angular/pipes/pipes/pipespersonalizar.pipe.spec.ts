import { PipespersonalizarPipe } from './pipespersonalizar.pipe';

describe('PipespersonalizarPipe', () => {
  it('create an instance', () => {
    const pipe = new PipespersonalizarPipe();
    expect(pipe).toBeTruthy();
  });
});
