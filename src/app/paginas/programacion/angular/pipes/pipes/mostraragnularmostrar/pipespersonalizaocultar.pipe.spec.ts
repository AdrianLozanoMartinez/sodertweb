import { PipespersonalizaocultarPipe } from './pipespersonalizaocultar.pipe';

describe('PipespersonalizaocultarPipe', () => {
  it('create an instance', () => {
    const pipe = new PipespersonalizaocultarPipe();
    expect(pipe).toBeTruthy();
  });
});
