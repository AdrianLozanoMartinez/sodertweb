import { PipespersonalizadaurlPipe } from './pipespersonalizadaurl.pipe';

describe('PipespersonalizadaurlPipe', () => {
  it('create an instance', () => {
    const pipe = new PipespersonalizadaurlPipe();
    expect(pipe).toBeTruthy();
  });
});
