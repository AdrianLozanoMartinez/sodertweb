import { Pipespersonalizar2Pipe } from './pipespersonalizar2.pipe';

describe('Pipespersonalizar2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Pipespersonalizar2Pipe();
    expect(pipe).toBeTruthy();
  });
});
