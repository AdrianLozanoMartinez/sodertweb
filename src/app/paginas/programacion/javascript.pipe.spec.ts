import { JavascriptPipe } from './javascript.pipe';

describe('JavascriptPipe', () => {
  it('create an instance', () => {
    const pipe = new JavascriptPipe();
    expect(pipe).toBeTruthy();
  });
});
